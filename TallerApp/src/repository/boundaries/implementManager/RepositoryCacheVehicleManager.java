package repository.boundaries.implementManager;

import repository.entities.VehicleRepo;

public interface RepositoryCacheVehicleManager {
	VehicleRepo insertCacheVehicleRepoManager(VehicleRepo vehicleRepo);
}
